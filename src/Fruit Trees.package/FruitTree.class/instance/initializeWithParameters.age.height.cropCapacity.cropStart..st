initialization
initializeWithParameters: aSpeciesName age: aAge height: aHeight cropCapacity: aCropCapacity cropStart: aCropStart
	"Initialize fruit tree with parameters"
	
	speciesName := aSpeciesName.
	age := aAge.
	height := aHeight.
	cropCapacity := aCropCapacity.
	cropStart := aCropStart.